# Security policy

Report vulnerabilities privately through GitHub Security Advisories for this
repository. Do not disclose credentials, certificates, private keys or device
configuration in a public issue.

Only the latest release is supported. Rotate the Management Suite signing key
and all affected device trust or mutual-TLS credentials after
suspected compromise. The offline artifact-signing private key must never be
copied into the add-on.
